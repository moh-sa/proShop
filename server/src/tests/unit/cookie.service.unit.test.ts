import { describe, suite } from "node:test";

import { CookieService } from "../../services/index.js";

suite(
	"Cookie Service〖 Unit Tests 〗",
	{ skip: true, todo: "IMPLEMENT" },
	() => {
		const service = new CookieService(); // eslint-disable-line @typescript-eslint/no-unused-vars

		// I AM TIRED BOSS :')
		describe("set", () => {});
		describe("get", () => {});
		describe("delete", () => {});
	},
);
